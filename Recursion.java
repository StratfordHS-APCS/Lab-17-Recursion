public class Recursion
{
    // return n!
    public static int factorial(int n)
    {

    }

    // return the greatest common divisor of x and y
    // example: gcd of 12 and 8 is 4
    public static int gcd(int x, int y)
    {

    }

    // return the nth fibonacci number
    // example: fib(10) = 55
    public static int fib(int n)
    {

    }

    /**
     * Run the recursive methods.
     *
     * @param args not used
     */
    public static void main( String[] args )
    {
        System.out.println( "10! = " + factorial(10) + " (should be 3628800)" );
        System.out.println( "GCD of 8 and 12 is " + gcd(8, 12 ) + " (should be 4)" );
        System.out.println( "The 10th fibonacci number is " + fib(10) + " (should be 55)" );
    }
}
